<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.softsociety.bacs.dao.AdminDAO">
    <select id="getStoreList" resultType="BacsStore">
        select * from BACS_STORE
    </select>
</mapper>